# 公開資料の判断記録

## 2026-09-07: 共通作業ルールの導入（Issue #1）

- AGENTS.md、CLAUDE.md、PRテンプレートを配置し、このリポジトリで独立して管理する。共通テンプレートの必要なルールをコピーし、外部との自動同期やグローバル設定変更は行わない。
- Issue・featureブランチ・PRの運用を維持し、公開範囲、本人とAIの担当、検証結果の条件と限界をプロジェクト固有ルールに追加する。
- 公開する素材は個別に選び、非公開のコードやデータ、内部情報を転載しない。PR本文・コメントも公開資料として確認する。
- 文書中心のリポジトリとして、参照・表示・内容の整合性を変更に応じて検証する。CIやアプリのビルド基盤は今回導入しない。

## 2026-09-07: AI Incident Investigatorの紹介記事（Issue #2）

- projects/ai-incident-investigator/README.mdに紹介記事を置き、ルートREADMEからリンクする。GitHub上で読めるMarkdownとMermaidを使い、サイトの実装・デプロイは追加しない。
- 課題、根拠をたどる操作、構成、設計判断、検証、担当とAI支援の順に説明する。OpenAIの任意の抽出要約とローカル分析を分け、原因確定や実運用の精度を主張しない。
- 確認した最新実装で合成デモの操作検証と撮影を行い、候補・時系列・再表示案内の3パネルだけを採用する。入力データファイルや実装コードは掲載しない。
- CI件数には実行日と照合日を添え、27合成シナリオがBackend検証の内数であることを示す。今回の操作検証と既存CIを区別し、未検証範囲も記載する。
- 本人の方針決定とCodexによる実装・テスト・レビュー支援を明記する。第三者レビューや実運用実績を作業の証拠なく付け加えない。

## 2026-09-07: Career Research Agentの紹介記事（Issue #5）

- projects/career-research-agent/README.mdに記事を追加し、ルートREADMEからリンクする。個人利用に合わせた構成の簡略化、有料APIの制御、保存失敗への備え、引用の検証を中心に説明する。
- 既存のデモモードで架空企業の入力・生成・履歴・再表示・Markdown出力を確認し、入力フォームとレポート上部の2画像を掲載する。実データ・実レポート・実装コードは持ち込まない。撮影時の外部通信と実DB作成がないことを確認する。
- 構成図は実調査の経路に限定し、AIとSQLiteを使わないデモとの違いを本文で明示する。
- 2026-09-07の成功した既存CIログと実装を照合し、自動テスト83件の内訳を記載する。過去の実API検証は企業名・求人URLの各1例とし、約11.8秒という観測値を性能保証にしない。記事作成では課金APIを呼ばない。
- 引用先と主張の対応不足、最新性の表現に課題が残ったこと、最後に補強した調査指示の効果は実生成で未検証であることを記す。本人の方針決定とCodexによる実装・検証・レビュー支援を区別する。
- 記事の相対リンク、画像参照・PNGメタデータ、公開範囲を確認し、ローカルのMarkdownプレビューで画像とMermaid図を描画する。文書・画像のみの変更として、アプリ機能やサイトのデプロイ、CI基盤は追加しない。
