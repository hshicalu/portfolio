# エージェント向け作業指示

## 作業開始

- 対象リポジトリのREADMEと作業指示を読み、Gitの状態を確認する。既存の未コミット変更を保持する。
- ファイル変更を伴う開発作業は、原則としてIssueを起点にする。既存Issueとコメントを確認し、該当Issueがなければ目的・スコープ・完了条件を記載して作成する。質問への回答・読み取りのみの調査ではIssue作成は不要。
- 新規作業は最新mainを起点にした `feature/<作業内容>` ブランチで行う。既存作業の継続では対応するブランチを使う。
- mainへ直接コミット・pushしない。空の新規リポジトリで履歴が必要な場合のみ、README等の最小初期化を許容し、以後の変更はfeatureブランチとPRで行う。

## 実装と検証

- Issueの範囲に変更を限定し、必要な実装判断を利用先の指定文書に記録する。
- 変更に応じたテスト・ビルドを実行する。文書のみの変更は参照先・差分・テンプレートの整合性を確認する。
- 検証失敗や未実行を成功扱いしない。原因と未完了事項をPRに明記する。
- 秘密情報や実環境の設定値をコミットしない。push前に対象ファイルと差分を確認する。

## PRとレビュー

- 作業ブランチへコミット・pushし、main向けのPRを作成する。既存の対応PRがあれば更新する。
- PR本文には原則 `Closes #<Issue番号>` を記載し、mainへのマージでIssueを自動closeする。
- Issueの完了条件を満たす単位でPRを作る。部分対応ではIssueの分割などを行い、未完了のIssueを誤ってcloseしない。
- Issueに紐づけない例外ではPR本文に理由を明記する。
- PRには変更内容・実装判断・検証結果・未検証事項を記載する。
- 作業途中はDraftを利用できる。実装と必要な検証が完了し、本文が整ったらReady for reviewにする。Draftのまま残す場合は理由を報告する。
- ユーザーから明示的にレビュー可能にするよう依頼された場合は、未検証事項を本文に残したうえでReady for reviewにできる。これは検証成功を意味しない。
- マージ・自動マージの有効化は、ユーザーから明示的に依頼された場合のみ実行する。

## 適用範囲

この文書は各リポジトリの共通の既定ルールです。プロジェクト固有の制約も読み、矛盾を放置しないでください。最新の明示的なユーザー指示を優先します。

## プロジェクト固有ルール

### 公開する資料

- README.mdの公開方針を仕様の起点とする。このリポジトリは公開用の紹介資料を管理する。
- 掲載対象は説明文、構成図、公開用の合成データから作った画面キャプチャ・デモとする。
- 非公開の実装コード、実運用データ、個人情報、APIキー、環境設定、内部URL、端末固有のパスを持ち込まない。Issue・PR本文やコメントにも適用する。
- 元リポジトリを丸ごとコピーしない。素材は掲載対象を個別に選び、画像内の文字とメタデータも確認する。端末全体の画面を掲載しない。

### 説明と根拠

- 本人の担当・意思決定とAI支援による作業を区別する。人間によるレビューや実運用成果を未確認のまま主張しない。
- テスト件数・計測値には確認日と対象を添える。合成ケースの回帰成功を実障害の正解率・網羅性・性能保証として扱わない。
- 想定、確認できた事実、未検証事項を区別し、現在の実装と異なる図・画像・説明は更新する。
- 記事と関係のない製品機能の開発、実APIへの課金を伴う検証、サイトのデプロイへ範囲を広げない。

### 編集と検証

- 記事はprojects/、記事ごとの画像はその配下のimages/に置き、README.mdから辿れるようにする。
- 公開資料の構成や掲載方針の判断はdocs/decisions.mdへ記録する。
- Markdownの相対リンクと画像参照、図の構文と表示、見出し、画像の可読性・公開範囲を確認する。
- 文書のみの変更で不要なアプリテストを追加しない。CIがない場合や未実行の検証を成功扱いせず、実際の確認内容をPRに記録する。
